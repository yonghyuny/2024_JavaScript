// 1번문제
// input: window.prompt
// output: html
// 유저에게 밑변과 높이를 입력을 받고 정삼각형의 넓이 나타내기

// 2번문제
// 한국 돈 원을 입력하면 엔화로 바꿔주기(1000원은 -> 114엔)

// const width = Number(window.prompt("밑변:"));
// const height = Number(window.prompt("높이:"));
// const area = document.createElement("div");
// area.innerText = `밑변 ${width} * 높이 ${height} = ${width * height * 0.5}`;
// document.body.appendChild(area);

// const won = Number(window.prompt("한국 돈:"));
// const yen = `${won * 0.114}`;
// const exchange = document.createElement("div");
// exchange.innerText = `${yen}`;
// document.body.appendChild(exchange);
