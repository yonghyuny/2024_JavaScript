const a = "hello";
// a.includes()
const b = 123;

const c = true;

const d = ["coffee", "bread", "icecream"];
// d.map((v) => {});

const e = document.createElement("div");
// e.appendChild();
const f = { name: "kim", age: 24 };
// f.key();

// type-casting
// 명시적(explicit): Number(),String(), Boolean(),Array(), Object()......
// 묵시적(implicit): +, !!, [], {}
